@AGENTS.md

## Claude Code

- Use `AGENTS.md` as the shared project instruction source to avoid drift between coding agents.
- Keep repository guidance concise and prefer updating shared docs instead of adding duplicated rules here.
